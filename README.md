# AI翻译工具 (AI Translator)

一个基于AI的i18n文件自动翻译CLI工具，支持增量更新和多语言翻译。

## 功能特性

- 🤖 **AI驱动翻译**: 使用先进的AI模型进行高质量翻译
- 📊 **增量更新**: 只翻译新增或修改的内容，避免重复翻译
- 🌍 **多语言支持**: 支持11种语言的自动翻译
- 📁 **智能分组**: 按功能模块分组翻译，保持上下文一致性
- 🔍 **预览模式**: 支持预览翻译内容，确认无误后再执行
- 📈 **状态检查**: 实时检查翻译状态和文件差异
- ⚡ **批量处理**: 一次性翻译所有目标语言

## 支持的语言

- 🇨🇳 简体中文 (zh-CN)
- 🇫🇷 法语 (fr)
- 🇩🇪 德语 (de)
- 🇪🇸 西班牙语 (es)
- 🇮🇹 意大利语 (it)
- 🇳🇱 荷兰语 (nl)
- 🇵🇱 波兰语 (pl)
- 🇸🇪 瑞典语 (se)
- 🇩🇰 丹麦语 (dk)
- 🇨🇿 捷克语 (cz)
- 🇧🇾 白俄罗斯语 (be)

## 安装和配置

### 1. 安装依赖

```bash
bun install
```

### 2. 配置API

在项目根目录创建 `.env` 文件：

```env
apikey = your_api_key_here
url = https://open.bigmodel.cn/api/paas/v4/chat/completions
module = glm-4.5
max_tokens = 4096
temperature = 0.6
```

### 3. 文件结构

确保你的项目有以下结构：

```
project/
├── .env                    # API配置文件
├── message/                # 语言文件目录
│   ├── en.json            # 源语言文件
│   ├── zh-CN.json         # 中文翻译文件
│   ├── fr.json            # 法语翻译文件
│   └── ...                # 其他语言文件
├── message/temp/           # 临时文件目录
│   └── en_old.json        # 历史版本文件
└── src/                    # 源代码目录
```

## 使用方法

### 基本命令

```bash
# 查看帮助
bun run src/cli.ts --help

# 初始化翻译环境
bun run src/cli.ts init

# 检查翻译状态
bun run src/cli.ts status

# 快速翻译
bun run src/cli.ts quick

# 完整翻译（带选项）
bun run src/cli.ts translate

# 预览翻译内容
bun run src/cli.ts preview

# 查看支持的语言
bun run src/cli.ts languages
```

### 高级用法

```bash
# 强制翻译所有内容（忽略增量更新）
bun run src/cli.ts translate --force

# 预览模式（只显示不执行）
bun run src/cli.ts translate --dry-run

# 指定目录
bun run src/cli.ts translate --message-dir ./locales --temp-dir ./temp

# 指定特定语言
bun run src/cli.ts translate --languages "zh-CN,fr,de"
```

## 工作流程

### 首次使用

1. **初始化环境**:
   ```bash
   bun run src/cli.ts init
   ```

2. **检查状态**:
   ```bash
   bun run src/cli.ts status
   ```

3. **预览翻译**:
   ```bash
   bun run src/cli.ts preview
   ```

4. **执行翻译**:
   ```bash
   bun run src/cli.ts translate
   ```

### 日常更新

1. **修改 `en.json` 文件**，添加新的翻译内容

2. **检查变化**:
   ```bash
   bun run src/cli.ts status
   ```

3. **增量翻译**（只翻译新增/修改的内容）:
   ```bash
   bun run src/cli.ts translate
   ```

## 翻译分组策略

工具会自动将 `en.json` 内容按以下策略分组：

- **common组**: 通用UI文本（按钮、状态、操作等）
- **form组**: 表单相关文本（验证、提示、标题等）
- **其他组**: 根据你的文件结构自动识别

每个组独立翻译，确保同组内翻译的一致性。

## 输出示例

```bash
🚀 AI翻译工具启动

=== 语言文件信息 ===
📄 源语言文件: en (✅)
🌍 目标语言数量: 10
📋 目标语言列表:
  - zh-CN (✅)
  - fr (✅)
  - de (✅)
  ...

📊 准备翻译 15 项内容到 10 种语言

🌍 开始翻译 简体中文 (zh-CN)
  📁 翻译组: common
    - loading: "Loading..." → "加载中..."
    - save: "Save" → "保存"
    ...
  ✅ 完成 common 组的翻译

==================================================
📊 翻译结果摘要
==================================================
✅ 翻译完成！
📈 统计信息:
  - 总语言数: 10
  - 已翻译: 10
  - 已跳过: 0
  - 错误数: 0
==================================================
```

## 配置说明

### API配置

- `apikey`: AI服务的API密钥
- `url`: AI服务的API端点
- `module`: 使用的AI模型
- `max_tokens`: 最大token数量
- `temperature`: 翻译的创造性程度（0-1）

### 翻译选项

- `--force`: 强制翻译所有内容
- `--dry-run`: 预览模式，不实际翻译
- `--message-dir`: 指定语言文件目录
- `--temp-dir`: 指定临时文件目录
- `--languages`: 指定要翻译的语言

## 故障排除

### 常见问题

1. **API密钥错误**: 检查 `.env` 文件中的 `apikey` 配置
2. **文件不存在**: 确保 `message/en.json` 文件存在
3. **权限问题**: 确保有读写 `message/` 目录的权限
4. **网络问题**: 检查网络连接和API端点可访问性

### 调试模式

使用预览模式进行调试：

```bash
bun run src/cli.ts translate --dry-run
```

## 开发和贡献

### 项目结构

```
src/
├── ai.ts              # AI翻译功能
├── cli.ts             # CLI入口
├── diff.ts            # 文件差异检测
├── file-processor.ts  # 文件处理
└── translate.ts       # 翻译协调
```

### 运行开发

```bash
# 开发模式
bun run dev

# 构建
bun run build

# 测试
bun test
```

## 许可证

MIT License

## 更新日志

### v1.0.0
- 初始版本发布
- 支持11种语言的AI翻译
- 实现增量更新功能
- 添加CLI界面
- 支持预览模式
