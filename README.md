# 使用说明

## 文件说明
en.json 是翻译源文件

en_old.json 记录的是发生变化前的内容， 上一次翻译流程结束后产生。

发生变化后的 en.json 与 en_old.json 进行对比，文件的差异决定翻译的内容。
- 如果缺少字段则移除其他语言文件的相应字段。
- 如果字段中的内容发生任意变化，则整个字段重新翻译，进行替换。
- 如果是新增字段，则进行翻译然后追加。

## 基本操作流程
用新的en.json覆盖已有的en.json文件
执行

```
bun run src/cli.ts auto
```